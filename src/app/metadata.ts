import type { Metadata } from "next";

export const metadata: Metadata = {
  title: {
    default: "Invoxa",
    template: "%s | Invoxa",
  },
  description: "Invoice and proposal generator for Nigerian freelancers",
};
